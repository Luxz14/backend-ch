//Callback
//Sincronismo y asincronismo

//Evolucion
//async => callbacks => promesas => async await(Sugar syntax);

const numerosArray = [1, 2, 3, 4];

const newArray = numerosArray.map(numero => {return numero * 2});

console.log(newArray)